# 🚀 دليل البدء السريع

<div dir="rtl">

## التثبيت في 3 خطوات

```bash
# 1. تثبيت المتطلبات
pip install -r requirements.txt

# 2. إعطاء صلاحية التنفيذ
chmod +x domain.py

# 3. تشغيل الأداة
python3 domain.py -d example.com --vulns
```

## أمثلة سريعة

### 1️⃣ فحص سريع للثغرات فقط

```bash
python3 domain.py -d target.com --vulns
```

**النتيجة:**
- فحص 12 نوع من الثغرات
- عرض رابط كل ثغرة مباشرة
- كود POC جاهز للتشغيل
- تقرير HTML ملون

### 2️⃣ فحص شامل كامل

```bash
python3 domain.py -d target.com --full
```

**يشمل:**
- ✅ معلومات النطاق
- ✅ النطاقات الفرعية
- ✅ اكتشاف المسارات
- ✅ لوحات التحكم
- ✅ سطح الهجوم
- ✅ الثغرات الأمنية

### 3️⃣ فحص مخصص

```bash
python3 domain.py -d target.com --info --subdomains --vulns --threads 20
```

## فهم النتائج

### عند اكتشاف ثغرة، ستظهر:

```
🔴 ثغرة مكتشفة: SQL Injection [Critical]
🔗 الرابط: https://target.com/page.php?id=1' OR '1'='1
✓ إثبات الثغرة متاح
```

### كود POC يظهر مباشرة:

```python
import requests

url = "https://target.com/page.php?id=1' OR '1'='1"
response = requests.get(url)

if "SQL syntax" in response.text:
    print("[+] ثغرة مؤكدة!")
```

## مستويات الخطورة

| المستوى | اللون | الوصف |
|---------|-------|-------|
| **Critical** | 🔴 أحمر غامق | ثغرات خطيرة جداً - تحتاج إصلاح فوري |
| **High** | 🟠 أحمر | ثغرات عالية الخطورة |
| **Medium** | 🟡 أصفر | ثغرات متوسطة الخطورة |
| **Low** | 🔵 أزرق | ثغرات منخفضة الخطورة |
| **Info** | ⚪ سماوي | معلومات إضافية |

## التقارير

بعد انتهاء الفحص، ستجد 3 ملفات في مجلد `results/`:

1. **report.html** - تقرير تفاعلي ملون (افتحه بالمتصفح)
2. **report.md** - تقرير Markdown
3. **report.txt** - تقرير نصي بسيط

### فتح التقرير:

```bash
# Linux/Mac
xdg-open results/report.html

# Windows
start results/report.html
```

## نصائح الاستخدام

### ⚡ زيادة السرعة

```bash
python3 domain.py -d target.com --vulns --threads 30
```

### 🎯 تحديد ملف الإخراج

```bash
python3 domain.py -d target.com --vulns --output my_scan
```

### ⏱️ إضافة تأخير بين الطلبات

```bash
python3 domain.py -d target.com --vulns --delay 0.5
```

### 🔍 وضع تفصيلي

```bash
python3 domain.py -d target.com --vulns --verbose
```

## استكشاف الأخطاء

### خطأ: Missing dependencies

```bash
pip install -r requirements.txt --upgrade
```

### خطأ: Connection timeout

```bash
python3 domain.py -d target.com --vulns --timeout 60
```

### خطأ: Permission denied

```bash
chmod +x domain.py
chmod +x install.sh
```

## ماذا تفعل بعد العثور على ثغرات؟

1. ✅ **وثّق الثغرة**: احفظ التقرير و POC
2. ✅ **تحقق يدوياً**: اختبر POC على بيئة آمنة
3. ✅ **أبلغ المسؤولين**: تواصل مع فريق الموقع
4. ✅ **لا تستغل الثغرة**: التزم بالأخلاقيات
5. ✅ **انتظر الإصلاح**: أعط وقتاً كافياً للإصلاح

## الحصول على مساعدة

### GitHub Issues
افتح Issue على: https://github.com/SaudiLinux/Domain/issues

### البريد الإلكتروني
SayerLinux@gmail.com

### المجتمع
انضم لمجتمع الأمن السيبراني العربي

## ⚠️ تذكير مهم

```
┌─────────────────────────────────────────┐
│  لا تستخدم الأداة على مواقع بدون إذن   │
│     الاستخدام غير المصرح به جريمة      │
│      احصل على إذن كتابي دائماً         │
└─────────────────────────────────────────┘
```

## الخطوات التالية

1. 📖 اقرأ [دليل الثغرات](VULNERABILITIES_GUIDE.md)
2. 🎓 تعلم كيفية إصلاح الثغرات
3. 🔬 جرب على مواقع تدريبية مثل:
   - DVWA (Damn Vulnerable Web Application)
   - WebGoat
   - HackTheBox
   - TryHackMe

---

<div align="center">

**صُنع بـ ❤️ | استخدم بمسؤولية 🔒**

[← العودة للدليل الرئيسي](README.md)

</div>

</div>
