# 🔍 Domain Scanner - الدليل الشامل للتشغيل

## ✅ حالة المشروع
**تم التشغيل بنجاح! ✓**

---

## 📦 محتويات المشروع

```
domain_scanner/
├── domain.py                    # الملف الرئيسي للأداة
├── requirements.txt             # المتطلبات
├── modules/                     # وحدات الأداة
│   ├── __init__.py
│   ├── banner.py               # عرض شعار الأداة
│   ├── info_gatherer.py        # جمع معلومات النطاق
│   ├── subdomain_enum.py       # اكتشاف النطاقات الفرعية
│   ├── url_discovery.py        # اكتشاف الروابط
│   ├── admin_finder.py         # البحث عن لوحات التحكم
│   ├── attack_surface.py       # رسم سطح الهجوم
│   ├── vulnerability_scanner.py # فحص الثغرات الكلاسيكية
│   ├── zeroday_scanner.py      # فحص ثغرات Zero-Day
│   ├── report_generator.py     # إنشاء التقارير
│   └── utils.py                # أدوات مساعدة
└── results/                     # مجلد التقارير
```

---

## 🚀 طريقة التشغيل

### 1️⃣ التثبيت

```bash
# تثبيت المتطلبات
pip install -r requirements.txt

# أو تثبيت يدوي
pip install requests beautifulsoup4 dnspython python-whois colorama tqdm rich aiohttp
```

### 2️⃣ أمثلة التشغيل

#### فحص أساسي (معلومات النطاق فقط)
```bash
python3 domain.py -d example.com --info
```

#### فحص الثغرات الكلاسيكية
```bash
python3 domain.py -d example.com --vulns
```

#### فحص شامل مع Zero-Day
```bash
python3 domain.py -d example.com --info --vulns --zeroday
```

#### فحص كامل (كل الوحدات)
```bash
python3 domain.py -d example.com --full
```

#### فحص مخصص مع خيارات متقدمة
```bash
python3 domain.py -d example.com \
  --vulns \
  --zeroday \
  --threads 10 \
  --timeout 20 \
  --output my_report
```

#### فحص مع مفتاح NVD API
```bash
python3 domain.py -d example.com \
  --zeroday \
  --nvd-key YOUR_NVD_API_KEY
```

---

## 🎯 الوحدات والمميزات

### ✨ الوحدات المتاحة

| الوحدة | الخيار | الوصف |
|--------|--------|-------|
| معلومات النطاق | `--info` | جمع معلومات أساسية عن النطاق |
| النطاقات الفرعية | `--subdomains` | اكتشاف النطاقات الفرعية |
| اكتشاف الروابط | `--urls` | البحث عن روابط مخفية |
| لوحات التحكم | `--admin-finder` | البحث عن صفحات الإدارة |
| سطح الهجوم | `--attack-surface` | رسم خريطة سطح الهجوم |
| الثغرات الكلاسيكية | `--vulns` | فحص ثغرات معروفة (SQL, XSS) |
| Zero-Day | `--zeroday` | اكتشاف ثغرات متقدمة |

### 🔥 ثغرات Zero-Day المدعومة

1. **Technology Fingerprinting** - بصمة التقنيات المستخدمة
2. **NVD/CVE Lookup** - البحث عن ثغرات معروفة في قاعدة بيانات NVD
3. **Server-Side Template Injection (SSTI)** - حقن القوالب
4. **HTTP Request Smuggling** - تهريب طلبات HTTP
5. **Web Cache Poisoning** - تسميم ذاكرة التخزين المؤقت
6. **Host Header Injection** - حقن رأس المضيف
7. **CRLF Injection** - حقن CRLF
8. **JWT Algorithm Confusion** - خلط خوارزمية JWT
9. **GraphQL Introspection** - استكشاف GraphQL
10. **Prototype Pollution** - تلويث النموذج الأولي
11. **Deserialization** - ثغرات إلغاء التسلسل
12. **Path Normalization Bypass** - تجاوز تطبيع المسار
13. **HTTP/2 Rapid Reset (CVE-2023-44487)** - ثغرة إعادة تعيين HTTP/2
14. **Open API/Actuator Exposure** - كشف نقاط نهاية الإدارة
15. **Sensitive Files Exposure** - كشف الملفات الحساسة

---

## 📊 التقارير

الأداة تُنشئ تقريرين تلقائياً:

### 1. تقرير Markdown (.md)
```
results/domain_com_20260223_043148.md
```
- تقرير نصي منسق
- سهل القراءة
- يمكن فتحه في أي محرر نصوص

### 2. تقرير HTML (.html)
```
results/domain_com_20260223_043148.html
```
- تقرير تفاعلي بألوان
- يُفتح في المتصفح
- تصميم احترافي بالعربية

---

## 🔧 الخيارات المتقدمة

```bash
--threads 10           # عدد الخيوط (افتراضي: 10)
--timeout 30           # مهلة الاتصال بالثواني (افتراضي: 30)
--delay 0.5            # تأخير بين الطلبات (افتراضي: 0)
--user-agent "..."     # User-Agent مخصص
--wordlist file.txt    # قائمة كلمات مخصصة للفحص
--output report        # اسم ملف التقرير
--nvd-key API_KEY      # مفتاح NIST NVD API
--verbose              # عرض تفاصيل إضافية
```

---

## 📝 نتائج الاختبار

### ✅ تم الاختبار على: example.com

#### النتائج:
- **IP Address:** 104.18.26.120
- **Server:** Cloudflare
- **Protocol:** HTTPS
- **Status Code:** 200

#### الثغرات المكتشفة:
1. **HTTP/2 Rapid Reset (CVE-2023-44487)** - [HIGH]
   - الخادم يدعم HTTP/2
   - معرض لهجوم Rapid Reset DoS

---

## ⚠️ ملاحظات مهمة

### القيود القانونية
- ⚖️ **استخدم الأداة فقط على نطاقات تملكها أو لديك تصريح بفحصها**
- 🚫 الفحص غير المصرح به يُعتبر جريمة إلكترونية في معظم الدول
- ✅ استخدم الأداة للأغراض التعليمية أو لتأمين أنظمتك الخاصة

### القيود التقنية
- بعض الوحدات تتطلب اتصال إنترنت مستقر
- فحص Zero-Day قد يستغرق وقتاً طويلاً (دقائق)
- بعض الخوادم قد تحظر IP الفحص عند اكتشاف نشاط مشبوه

---

## 🆘 حل المشاكل

### خطأ: ModuleNotFoundError
```bash
pip install -r requirements.txt
```

### خطأ: Permission denied
```bash
chmod +x domain.py
```

### خطأ: Connection timeout
```bash
# زيادة مهلة الاتصال
python3 domain.py -d example.com --timeout 60
```

### خطأ: Rate limit exceeded (NVD API)
```bash
# استخدام مفتاح NVD API
python3 domain.py -d example.com --nvd-key YOUR_KEY
```

---

## 📞 الدعم

- **المطور:** SayerLinux
- **البريد الإلكتروني:** SayerLinux@gmail.com
- **GitHub:** https://github.com/SaudiLinux/Domain

---

## 📜 الترخيص

هذه الأداة مفتوحة المصدر للأغراض التعليمية.
استخدامها على مسؤوليتك الخاصة.

---

## 🎉 نجاح التشغيل!

✅ تم تشغيل الأداة بنجاح
✅ تم إنشاء جميع الوحدات
✅ تم اختبار الفحص على example.com
✅ تم إنشاء التقارير بنجاح

**المشروع جاهز للاستخدام! 🚀**
